# Supabase — get these from the Supabase project dashboard (Settings → API)
# Public values are safe to expose in the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secret. NEVER expose to the browser. Used by admin/CRM server actions.
SUPABASE_SERVICE_ROLE_KEY=

# Public site URL (used for metadata / redirects), e.g. https://christiesgolfranch.com
NEXT_PUBLIC_SITE_URL=
